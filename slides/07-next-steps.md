# Próximos Passos

- Substituir `Scan` por `Query` + GSI para `GetEvents` em produção
- Validar conversões de `float64`→`int64` e regras de rounding
- Adicionar tracing distribuído (OpenTelemetry)
- Preparar PRs com melhorias e testes adicionais
